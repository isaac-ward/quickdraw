name: recorded    # env-registry name (environments/registry.make_env) — owm-envs ISS docking datasets, no simulator
obs_dim: 13       # ISS chaser state: pos(3), vel(3), quat(4), body rate(3)
action_dim: 6     # body-frame force(3) + torque(3)
dt: 0.05          # 20 Hz
