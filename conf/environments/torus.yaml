R: 0.75           # ~1-unit torus (extent R+r = 1.0 in x/y)
r: 0.25
dt: 0.0166667     # 60 Hz — smaller integration step
gamma: 0.1
a_max: 2.0
init_speed: 1.0
mass: 1.3         # inertia: action / mass, so higher = harder to push around
