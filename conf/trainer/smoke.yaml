max_epochs: 3      # just enough to exercise the curriculum + checkpoint + eval-callback path
precision: bf16-mixed
save_top_k: 1
devices: 1
monitor: val/metric/proprio/manifold_distance_error
monitor_mode: min
validation_metrics: torus
accumulate_grad_batches: 1
check_val_every_n_epoch: 1
