{
 "note": "eval_ood_horizon on the deployed flow checkpoint, +model.diffusion.stochastic_eval=false. DETERMINISM IS THE POINT: at the default stochastic_eval=true this eval's own run-to-run spread is 4.2e-3, LARGER than a real change would move it, so the gate must pin the sampling off. Pinned, two runs of identical code agree to 0.0e0. Captured from the code BEFORE the head-configurability change; values are the raw floats, not retyped.",
 "cmd": "scratch/_regress_eval.sh",
 "metrics": {
  "eval_ood_horizon/open_loop/proprio/pointwise_error": 1.2578097581863403,
  "eval_ood_horizon/closed_loop_1_steps/proprio/pointwise_error": 0.41914376616477966,
  "eval_ood_horizon/closed_loop_16_steps/proprio/pointwise_error": 0.7536526918411255
 }
}